# Copy to ".env" and fill in values (see README or project docs).
# Do not commit .env.

PORT=3000
LOG_LEVEL=info

# YouTube Data API v3 key (Google Cloud Console → APIs & Services → Credentials)
YOUTUBE_API_KEY=
